namespace Outbreak.Jobs
{
    /**
     * IMPORTANT: the order of the jobs must be in increasing order
     */
    public enum JobPriority : short
    {
        Low,
        Medium,
        High,
    }
}
